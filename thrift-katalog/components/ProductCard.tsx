import Link from "next/link";
import type { Product } from "@/lib/types";
export function ProductCard({product}:{product:Product}){return <Link href={`/produk/${product.id}`} className="group overflow-hidden rounded-2xl bg-white ring-1 ring-black/10"><div className="aspect-[4/5] overflow-hidden bg-neutral-100"><img src={product.images?.[0] || "https://placehold.co/900x1100/png?text=No+Image"} alt={product.name} className="h-full w-full object-cover transition duration-300 group-hover:scale-105"/></div><div className="p-4"><p className="text-xs uppercase tracking-wider text-neutral-500">{product.category}</p><h3 className="mt-1 font-bold">{product.name}</h3><p className="mt-2 font-semibold">Rp{Number(product.price||0).toLocaleString("id-ID")}</p>{!product.available&&<span className="mt-2 inline-block text-xs text-red-600">Habis</span>}</div></Link>}
