"use client";
import { useMemo, useState } from "react";
import type { Product } from "@/lib/types";
import { ProductCard } from "./ProductCard";
export function Catalog({products}:{products:Product[]}){const[q,setQ]=useState("");const[c,setC]=useState("Semua");const cats=["Semua",...Array.from(new Set(products.map(p=>p.category).filter(Boolean)))];const filtered=useMemo(()=>products.filter(p=>(c==="Semua"||p.category===c)&&p.name.toLowerCase().includes(q.toLowerCase())),[products,q,c]);return <section className="mx-auto max-w-6xl px-4 py-12"><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><h1 className="text-3xl font-black">Katalog</h1><p className="mt-1 text-neutral-500">Pilih produk yang kamu suka.</p></div><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Cari produk..." className="rounded-xl border bg-white px-4 py-3 outline-none"/></div><div className="mt-6 flex flex-wrap gap-2">{cats.map(x=><button key={x} onClick={()=>setC(x)} className={`rounded-full px-4 py-2 text-sm ${c===x?"bg-black text-white":"bg-white ring-1 ring-black/10"}`}>{x}</button>)}</div><div className="mt-8 grid grid-cols-2 gap-4 md:grid-cols-3 lg:grid-cols-4">{filtered.map(p=><ProductCard key={p.id} product={p}/>)}</div>{!filtered.length&&<p className="py-20 text-center text-neutral-500">Belum ada produk.</p>}</section>}
