"use client";
import { useEffect,useState } from "react";
import { signInWithEmailAndPassword,onAuthStateChanged } from "firebase/auth";
import { auth } from "@/lib/firebase";
import { isAdmin } from "@/lib/admin";
import { useRouter } from "next/navigation";
export default function Login(){const[e,setE]=useState("");const[p,setP]=useState("");const[err,setErr]=useState("");const[loading,setLoading]=useState(false);const r=useRouter();useEffect(()=>{const unsub=onAuthStateChanged(auth,async u=>{if(u&&(await isAdmin(u.uid)))r.replace("/admin")});return()=>unsub()},[r]);async function login(x:React.FormEvent){x.preventDefault();setErr("");setLoading(true);try{const cred=await signInWithEmailAndPassword(auth,e,p);if(!(await isAdmin(cred.user.uid))){await auth.signOut();throw new Error("Akun ini belum diberi akses admin.")}r.replace("/admin")}catch(error){setErr(error instanceof Error?error.message:"Login gagal. Periksa email dan password.")}finally{setLoading(false)}}return <main className="mx-auto max-w-md px-4 py-20"><h1 className="text-3xl font-black">Admin Login</h1><p className="mt-2 text-sm text-neutral-500">Kelola katalog dari laptop atau HP.</p><form onSubmit={login} className="mt-8 space-y-4"><input type="email" placeholder="Email admin" value={e} onChange={x=>setE(x.target.value)} className="w-full rounded-xl border bg-white px-4 py-3" required/><input type="password" placeholder="Password" value={p} onChange={x=>setP(x.target.value)} className="w-full rounded-xl border bg-white px-4 py-3" required/>{err&&<p className="rounded-xl bg-red-50 p-3 text-sm text-red-700">{err}</p>}<button disabled={loading} className="w-full rounded-xl bg-black px-4 py-3 font-bold text-white disabled:opacity-50">{loading?"Memeriksa...":"Masuk"}</button></form></main>}
